namespace MicroVerse.Helper
{
    // possible return values for UserHelper and PostHelper methods
    public enum Status
    {
        NotFound,
        NoContent,
        BadRequest,
        Redirect
    }
}
