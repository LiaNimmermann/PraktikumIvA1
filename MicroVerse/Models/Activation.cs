namespace MicroVerse.Models
{
    // the activation of a post or user
    public enum Activation
    {
        active = 0,
        flagged = 1,
        blocked = 2,
        deleted = 3,
        banned = 4
    }
}
